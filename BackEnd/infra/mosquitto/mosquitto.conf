# Configuration Mosquitto — DÉVELOPPEMENT UNIQUEMENT
# (Mosquitto 2.x refuse les connexions anonymes par défaut.)

listener 1883
allow_anonymous true
persistence false

# En production : désactiver l'anonymat, activer l'authentification (password_file) et TLS.
